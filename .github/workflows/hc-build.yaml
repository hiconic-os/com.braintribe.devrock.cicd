name: Build HC Build App

on:
  workflow_dispatch:

jobs:
  run:
    uses: hiconic-os/hiconic.ci.workflows/.github/workflows/range.yaml@main
    secrets: inherit
    with:
      range: "[devrock-cicd-app]"